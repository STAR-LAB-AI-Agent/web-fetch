[pytest]
# 说明：部分受限环境中系统临时目录不可写（或被安全软件锁定），会导致
# pytest 在创建默认临时目录时报 PermissionError。这里把测试临时目录
# 固定到仓库内，并关闭 pytest 自身的缓存写入，保证在任意机器上直接
# 执行 `pytest` 都能跑通。
addopts = -p no:cacheprovider --basetemp=.pytest_tmp
testpaths = tests
