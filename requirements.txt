# 核心开源依赖（第 22 题 AI 网页信息采集 · 初版）
# 主开源项目：Playwright Python —— 浏览器自动化与页面采集
playwright>=1.40
Flask>=3.0           # 本地模拟目标网站（演示/测试目标）

# 智能体在线解析（可选；未配置则使用离线规则解析）
openai>=1.0          # 兼容 OpenAI 接口的模型服务客户端
python-dotenv>=1.0   # 自动加载 .env 中的 OPENAI_* 配置

# 测试
pytest>=7.0